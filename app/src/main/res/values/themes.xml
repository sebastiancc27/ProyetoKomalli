<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Komalli" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorAccent">@color/primary_green</item>

    </style>

    <style name="Theme.Komalli" parent="Base.Theme.Komalli" />
    <style name="App.Custum.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/primary_blue</item>
    </style>

</resources>